name: Staging Deploy (install / demo / nocturnal / raindrop scripts)

on:
  push:
    branches:
      - master
    paths:
      - 'shell/probe/**'
  release:
    types:
      - published
  workflow_dispatch:
  pull_request:
    branches:
      - master

jobs:
  build_ubuntu:

    runs-on: ubuntu-latest
    environment: staging

    name: Staging Deploy (install / demo / nocturnal / raindrop scripts)

    steps:
      - name: Check out code. 
        uses: actions/checkout@v3
      
      - id: shell_probes_composite
        uses: libraries/shell_probes@v1
        with:
          environment: staging
          s3_bucket: ${{ secrets.BUCKET_NAME }}
          aws_region: ${{ secrets.BUCKET_REGION }}
          aws_access_key_id: ${{ secrets.STAGING_S3_DEPLOYMENT_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.STAGING_S3_DEPLOYMENT_SECRET_KEY  }}
          slack_workflow_url: ${{ secrets.SLACK_GIT_DEPLOYMENT_WORKFLOW }}