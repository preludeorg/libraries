name: Test Javascript

on:
  pull_request:
    branches:
      - master
    paths:
      - 'tests/**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup Node
      uses: actions/setup-node@v3
    - run: yarn install
    - run: yarn test